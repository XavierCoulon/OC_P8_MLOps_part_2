# Application settings
APP_NAME=Rugby MLOps API
APP_VERSION=0.1.0
DEBUG=True
API_PREFIX=/api/v1

# Security - REQUIRED
API_KEY=your-secret-api-key-here

# Hugging Face Model - REQUIRED
HF_REPO_ID=XavierCoulon/rugby-kicks-model
# Hugging Face API URL for inference (optional, for batch predictions)
HF_API_URI=https://your-space.hf.space/api/v1

# Database - REQUIRED
# For Docker (PostgreSQL):
# DATABASE_URL=postgresql+psycopg://admin:password@postgres:5432/rugby_kicks_db
# For Local (SQLite):
# DATABASE_URL=sqlite:///./rugby_local.db
# For Production (Neon):
# DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require
DATABASE_URL=

# EvidentlyAI settings (optional, for drift monitoring)
EVIDENTLY_PROJECT_ID=
EVIDENTLY_CLOUD_TOKEN=
EVIDENTLY_API_KEY=
